#include <stdio.h>

int main() {
    int num1;
    scanf("%d",&num);
    if(num > 0){
        printf("Positive %d",num);
    }
    else(num < 0){
        printf("Negative %d",num);
    }
    return 0;
}