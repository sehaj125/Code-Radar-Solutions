#include <stdio.h>

int main() {
    int age;
    scanf("%d",&age);
    if(age>=60){
        printf("Eligible",num);
    }
    else{
        print("Not Eligible",num);
    }
    return 0;
}