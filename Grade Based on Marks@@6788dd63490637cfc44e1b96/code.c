#include <stdio.h>

int main() {
    int main(){
        int grade;
        scanf("%d",&grade);
        if(grade>=90){
            printf("A",grade);
        }
        if else(80<=grade<=90){
            printf("B",grade);
        }
        if else(70<=grade<80){
            printf("C",grade);
        }
        if else(60<=grade<70){
            printf("D",grade);
        }
        if else(grade<60){
            printf("F",grade);
        }
    }
    return 0;
}